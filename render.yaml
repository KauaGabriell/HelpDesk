services:
  - type: web
    name: helpdesk-api
    runtime: node
    plan: free
    rootDir: api
    buildCommand: npm ci && npx prisma generate && npx prisma migrate deploy && npm run db:bootstrap && npm run build
    startCommand: npm start
    healthCheckPath: /health
    autoDeployTrigger: commit
    envVars:
      - key: NODE_VERSION
        value: "22"
      - key: DATABASE_URL
        fromDatabase:
          name: helpdesk-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGINS
        value: https://help-desk-lovat.vercel.app,http://localhost:5173,http://localhost:5174
      - key: BOOTSTRAP_ADMIN_EMAIL
        sync: false
      - key: BOOTSTRAP_ADMIN_PASSWORD_HASH
        sync: false

databases:
  - name: helpdesk-db
    plan: free
    databaseName: helpdeskdb
    postgresMajorVersion: "18"
